<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Robica Store — نظام إدارة العملاء</title>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;900&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">

<!-- Firebase -->
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import { getFirestore, collection, addDoc, getDocs, query, where, orderBy, deleteDoc, doc, updateDoc } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";

  // =============================================
  // 🔥 ضع هنا إعدادات Firebase الخاصة بك
  // =============================================
  const firebaseConfig = {
    apiKey: "AIzaSyAlYG8k7fN3TULQzg70SVidNgDPCLYonVU",
    authDomain: "robica-store-db.firebaseapp.com",
    databaseURL: "https://robica-store-db-default-rtdb.firebaseio.com",
    projectId: "robica-store-db",
    storageBucket: "robica-store-db.firebasestorage.app",
    messagingSenderId: "578770796845",
    appId: "1:578770796845:web:c20991ae8c69c09da053a4"
  };

  const app = initializeApp(firebaseConfig);
  const db = getFirestore(app);

  // Make db available globally
  window.db = db;
  window.firestore = { collection, addDoc, getDocs, query, where, orderBy, deleteDoc, doc, updateDoc };
  window.firebaseReady = true;
  document.dispatchEvent(new Event('firebaseReady'));
</script>

<style>
  :root {
    --gold: #C9A84C;
    --gold-light: #E8CC82;
    --gold-dark: #9A7A2E;
    --black: #0A0A0A;
    --dark: #111111;
    --dark2: #1A1A1A;
    --dark3: #242424;
    --dark4: #2E2E2E;
    --white: #F5F0E8;
    --white2: #EDE8DC;
    --text-muted: #888880;
    --red: #C0392B;
    --green: #27AE60;
    --radius: 2px;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'Tajawal', sans-serif;
    background: var(--black);
    color: var(--white);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── BACKGROUND ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 60% 40% at 20% 10%, rgba(201,168,76,0.07) 0%, transparent 70%),
      radial-gradient(ellipse 40% 60% at 80% 90%, rgba(201,168,76,0.05) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── HEADER ── */
  .header {
    position: fixed;
    top: 0; left: 0; right: 0;
    height: 64px;
    background: rgba(10,10,10,0.95);
    border-bottom: 1px solid rgba(201,168,76,0.2);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 32px;
    z-index: 100;
    backdrop-filter: blur(12px);
  }

  .header-logo {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    color: var(--gold);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .header-logo span {
    color: var(--white);
    font-size: 11px;
    font-family: 'Tajawal', sans-serif;
    display: block;
    letter-spacing: 3px;
    font-weight: 300;
    margin-top: -4px;
  }

  .header-right {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .lang-toggle {
    background: transparent;
    border: 1px solid rgba(201,168,76,0.3);
    color: var(--gold);
    padding: 4px 12px;
    border-radius: 2px;
    cursor: pointer;
    font-family: 'Tajawal', sans-serif;
    font-size: 12px;
    transition: all 0.2s;
  }
  .lang-toggle:hover { background: rgba(201,168,76,0.1); }

  .employee-badge {
    background: rgba(201,168,76,0.1);
    border: 1px solid rgba(201,168,76,0.3);
    color: var(--gold-light);
    padding: 4px 14px;
    border-radius: 2px;
    font-size: 13px;
    display: none;
  }

  /* ── PAGES ── */
  .page {
    display: none;
    min-height: 100vh;
    padding-top: 64px;
    position: relative;
    z-index: 1;
  }
  .page.active { display: flex; flex-direction: column; align-items: center; justify-content: center; }

  /* ── LOGIN PAGE ── */
  #page-login {
    background: linear-gradient(135deg, #0A0A0A 0%, #111111 100%);
  }

  .login-box {
    width: 100%;
    max-width: 420px;
    padding: 48px 40px;
    background: var(--dark2);
    border: 1px solid rgba(201,168,76,0.2);
    position: relative;
    animation: fadeUp 0.6s ease both;
  }

  .login-box::before {
    content: '';
    position: absolute;
    top: -1px; left: 40px; right: 40px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  .login-title {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    color: var(--gold);
    text-align: center;
    margin-bottom: 4px;
  }

  .login-subtitle {
    text-align: center;
    color: var(--text-muted);
    font-size: 13px;
    margin-bottom: 40px;
    letter-spacing: 1px;
  }

  .form-group {
    margin-bottom: 20px;
  }

  label {
    display: block;
    font-size: 12px;
    color: var(--gold-light);
    margin-bottom: 8px;
    letter-spacing: 1px;
    font-weight: 500;
    text-transform: uppercase;
  }

  input, select {
    width: 100%;
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.15);
    color: var(--white);
    padding: 12px 16px;
    font-family: 'Tajawal', sans-serif;
    font-size: 15px;
    outline: none;
    border-radius: var(--radius);
    transition: border-color 0.2s;
    direction: rtl;
  }

  input:focus, select:focus {
    border-color: var(--gold);
    background: var(--dark4);
  }

  select option { background: var(--dark3); }

  .btn {
    width: 100%;
    padding: 14px;
    background: var(--gold);
    color: var(--black);
    border: none;
    font-family: 'Tajawal', sans-serif;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    border-radius: var(--radius);
    transition: all 0.2s;
    letter-spacing: 1px;
    margin-top: 8px;
  }
  .btn:hover { background: var(--gold-light); transform: translateY(-1px); }
  .btn:active { transform: translateY(0); }

  .btn-outline {
    background: transparent;
    border: 1px solid var(--gold);
    color: var(--gold);
  }
  .btn-outline:hover { background: rgba(201,168,76,0.1); transform: translateY(-1px); }

  .btn-red {
    background: var(--red);
    color: white;
  }
  .btn-red:hover { background: #e74c3c; }

  .btn-sm {
    width: auto;
    padding: 8px 20px;
    font-size: 13px;
    margin-top: 0;
  }

  /* ── HOME PAGE ── */
  #page-home {
    justify-content: center;
    padding: 80px 24px;
    gap: 24px;
  }

  .home-title {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    color: var(--gold);
    text-align: center;
    animation: fadeUp 0.5s ease both;
  }

  .home-subtitle {
    color: var(--text-muted);
    text-align: center;
    font-size: 14px;
    animation: fadeUp 0.5s 0.1s ease both;
  }

  .home-cards {
    display: flex;
    gap: 20px;
    margin-top: 16px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .home-card {
    width: 260px;
    padding: 40px 32px;
    background: var(--dark2);
    border: 1px solid rgba(201,168,76,0.15);
    cursor: pointer;
    transition: all 0.3s;
    text-align: center;
    position: relative;
    overflow: hidden;
    animation: fadeUp 0.5s ease both;
  }

  .home-card:nth-child(2) { animation-delay: 0.1s; }

  .home-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(201,168,76,0.05), transparent);
    opacity: 0;
    transition: opacity 0.3s;
  }

  .home-card:hover {
    border-color: var(--gold);
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.5);
  }
  .home-card:hover::after { opacity: 1; }

  .card-icon {
    font-size: 48px;
    margin-bottom: 20px;
  }

  .card-title {
    font-size: 20px;
    font-weight: 700;
    color: var(--gold);
    margin-bottom: 8px;
  }

  .card-desc {
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* ── EMPLOYEE SELECT PAGE ── */
  #page-employee {
    padding: 80px 24px;
    gap: 24px;
  }

  .page-title {
    font-size: 22px;
    font-weight: 700;
    color: var(--gold);
    text-align: center;
  }

  .employee-list {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    justify-content: center;
    max-width: 600px;
    animation: fadeUp 0.4s ease both;
  }

  .employee-btn {
    padding: 14px 28px;
    background: var(--dark2);
    border: 1px solid rgba(201,168,76,0.2);
    color: var(--white);
    font-family: 'Tajawal', sans-serif;
    font-size: 15px;
    cursor: pointer;
    border-radius: var(--radius);
    transition: all 0.2s;
    font-weight: 500;
  }
  .employee-btn:hover, .employee-btn.selected {
    border-color: var(--gold);
    color: var(--gold);
    background: rgba(201,168,76,0.08);
  }

  .add-employee-row {
    display: flex;
    gap: 10px;
    max-width: 400px;
    width: 100%;
    animation: fadeUp 0.4s 0.1s ease both;
  }

  .add-employee-row input {
    flex: 1;
  }

  /* ── NEW / EXISTING CUSTOMER FORM ── */
  #page-new-customer, #page-sale, #page-existing-search {
    padding: 80px 24px 40px;
    align-items: center;
  }

  .form-box {
    width: 100%;
    max-width: 520px;
    background: var(--dark2);
    border: 1px solid rgba(201,168,76,0.15);
    padding: 40px;
    position: relative;
    animation: fadeUp 0.4s ease both;
  }

  .form-box::before {
    content: '';
    position: absolute;
    top: -1px; left: 60px; right: 60px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  .form-title {
    font-size: 20px;
    font-weight: 700;
    color: var(--gold);
    margin-bottom: 28px;
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(201,168,76,0.1);
  }

  .row-2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .optional-tag {
    color: var(--text-muted);
    font-size: 10px;
    text-transform: none;
    font-weight: 400;
    margin-right: 4px;
  }

  .price-display {
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.2);
    padding: 20px;
    margin: 20px 0;
    border-radius: var(--radius);
  }

  .price-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 6px 0;
    font-size: 14px;
    color: var(--text-muted);
  }

  .price-row.final {
    border-top: 1px solid rgba(201,168,76,0.2);
    margin-top: 8px;
    padding-top: 14px;
    font-size: 20px;
    font-weight: 700;
    color: var(--gold);
  }

  .price-row .val { color: var(--white); }
  .price-row.final .val { color: var(--gold); }

  .discount-val { color: var(--red) !important; }

  /* ── CUSTOMER SEARCH ── */
  .search-result {
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.15);
    padding: 16px;
    margin-top: 12px;
    cursor: pointer;
    transition: all 0.2s;
    border-radius: var(--radius);
  }
  .search-result:hover { border-color: var(--gold); }
  .search-result .res-name { font-weight: 600; color: var(--gold); font-size: 15px; }
  .search-result .res-phone { color: var(--text-muted); font-size: 13px; margin-top: 4px; }
  .search-result .res-purchases { color: var(--white2); font-size: 12px; margin-top: 4px; }

  /* ── HISTORY PAGE ── */
  #page-history {
    padding: 80px 16px 40px;
    align-items: center;
  }

  .history-box {
    width: 100%;
    max-width: 800px;
    animation: fadeUp 0.4s ease both;
  }

  .history-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;
    flex-wrap: gap;
    gap: 12px;
  }

  .history-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
  }

  .history-table th {
    background: var(--dark3);
    color: var(--gold);
    padding: 12px 14px;
    text-align: right;
    font-weight: 600;
    font-size: 12px;
    letter-spacing: 0.5px;
    border-bottom: 1px solid rgba(201,168,76,0.2);
  }

  .history-table td {
    padding: 12px 14px;
    border-bottom: 1px solid rgba(255,255,255,0.04);
    color: var(--white2);
    vertical-align: middle;
  }

  .history-table tr:hover td { background: rgba(201,168,76,0.03); }

  .badge {
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 11px;
    font-weight: 600;
  }
  .badge-new { background: rgba(39,174,96,0.15); color: var(--green); border: 1px solid rgba(39,174,96,0.3); }
  .badge-existing { background: rgba(201,168,76,0.1); color: var(--gold); border: 1px solid rgba(201,168,76,0.2); }

  .delete-btn {
    background: transparent;
    border: 1px solid rgba(192,57,43,0.3);
    color: var(--red);
    padding: 4px 12px;
    border-radius: var(--radius);
    cursor: pointer;
    font-family: 'Tajawal', sans-serif;
    font-size: 12px;
    transition: all 0.2s;
  }
  .delete-btn:hover { background: rgba(192,57,43,0.1); }

  .filter-row {
    display: flex;
    gap: 10px;
    margin-bottom: 16px;
    flex-wrap: wrap;
  }

  .filter-row input, .filter-row select {
    flex: 1;
    min-width: 150px;
  }

  /* ── TOAST ── */
  .toast {
    position: fixed;
    bottom: 32px;
    left: 50%;
    transform: translateX(-50%) translateY(100px);
    background: var(--dark3);
    border: 1px solid var(--gold);
    color: var(--white);
    padding: 14px 28px;
    border-radius: var(--radius);
    font-size: 14px;
    z-index: 999;
    transition: transform 0.3s ease;
    white-space: nowrap;
  }
  .toast.show { transform: translateX(-50%) translateY(0); }
  .toast.success { border-color: var(--green); color: var(--green); }
  .toast.error { border-color: var(--red); color: var(--red); }

  /* ── MODAL ── */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.8);
    z-index: 200;
    display: none;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(4px);
  }
  .modal-overlay.open { display: flex; }

  .modal {
    background: var(--dark2);
    border: 1px solid rgba(201,168,76,0.3);
    padding: 40px;
    max-width: 380px;
    width: 90%;
    animation: fadeUp 0.3s ease both;
    position: relative;
  }

  .modal h3 { color: var(--gold); font-size: 18px; margin-bottom: 12px; }
  .modal p { color: var(--text-muted); font-size: 14px; margin-bottom: 24px; line-height: 1.6; }
  .modal-btns { display: flex; gap: 12px; }
  .modal-btns .btn { flex: 1; margin-top: 0; }

  /* ── BACK BTN ── */
  .back-btn {
    position: fixed;
    bottom: 24px;
    left: 24px;
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.2);
    color: var(--text-muted);
    padding: 10px 20px;
    border-radius: var(--radius);
    cursor: pointer;
    font-family: 'Tajawal', sans-serif;
    font-size: 13px;
    z-index: 50;
    transition: all 0.2s;
    display: none;
  }
  .back-btn:hover { color: var(--gold); border-color: var(--gold); }

  /* ── NAV TABS ── */
  .nav-tabs {
    display: flex;
    gap: 0;
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.15);
    border-radius: var(--radius);
    padding: 4px;
    margin-bottom: 20px;
  }

  .nav-tab {
    flex: 1;
    padding: 8px 20px;
    background: transparent;
    border: none;
    color: var(--text-muted);
    font-family: 'Tajawal', sans-serif;
    font-size: 13px;
    cursor: pointer;
    border-radius: 1px;
    transition: all 0.2s;
    text-align: center;
  }
  .nav-tab.active {
    background: var(--gold);
    color: var(--black);
    font-weight: 700;
  }

  /* ── EMPTY STATE ── */
  .empty-state {
    text-align: center;
    padding: 60px 20px;
    color: var(--text-muted);
  }
  .empty-state .emoji { font-size: 48px; margin-bottom: 16px; }
  .empty-state p { font-size: 14px; }

  /* ── CUSTOMER PROFILE ── */
  .customer-profile {
    background: var(--dark3);
    border: 1px solid rgba(201,168,76,0.2);
    padding: 20px;
    margin-bottom: 20px;
    border-radius: var(--radius);
  }

  .profile-name { font-size: 20px; font-weight: 700; color: var(--gold); margin-bottom: 6px; }
  .profile-info { display: flex; gap: 20px; flex-wrap: wrap; }
  .profile-info span { font-size: 13px; color: var(--text-muted); }
  .profile-info span b { color: var(--white); }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ── MOBILE ── */
  @media (max-width: 600px) {
    .header { padding: 0 16px; }
    .home-card { width: 100%; max-width: 320px; }
    .form-box { padding: 28px 20px; }
    .row-2 { grid-template-columns: 1fr; }
    .history-table { font-size: 12px; }
    .history-table th, .history-table td { padding: 10px 8px; }
    .login-box { padding: 36px 24px; }
    .modal { padding: 28px 20px; }
  }

  /* ── SCROLLBAR ── */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--dark); }
  ::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 3px; }

  .required-star { color: var(--red); margin-right: 2px; }
  .info-text { font-size: 12px; color: var(--text-muted); margin-top: 6px; }

  .sale-total-big {
    text-align: center;
    font-size: 36px;
    font-weight: 900;
    color: var(--gold);
    margin: 8px 0;
  }

  .divider {
    height: 1px;
    background: rgba(201,168,76,0.1);
    margin: 24px 0;
  }

  .sheet-notice {
    background: rgba(39,174,96,0.08);
    border: 1px solid rgba(39,174,96,0.2);
    padding: 12px 16px;
    border-radius: var(--radius);
    font-size: 12px;
    color: var(--green);
    margin-top: 16px;
    text-align: center;
  }

  .sheet-notice.warning {
    background: rgba(201,168,76,0.08);
    border-color: rgba(201,168,76,0.2);
    color: var(--gold);
  }
</style>
</head>
<body>

<!-- HEADER -->
<header class="header">
  <div class="header-logo">
    Robica
    <span>STORE</span>
  </div>
  <div class="header-right">
    <span class="employee-badge" id="employeeBadge">👤 <span id="employeeName"></span></span>
    <button class="lang-toggle" onclick="toggleLang()">EN / عر</button>
  </div>
</header>

<!-- BACK BUTTON -->
<button class="back-btn" id="backBtn" onclick="goBack()">→ رجوع</button>

<!-- ================================ -->
<!-- PAGE: LOGIN -->
<!-- ================================ -->
<div class="page active" id="page-login">
  <div class="login-box">
    <div class="login-title">Robica Store</div>
    <div class="login-subtitle" data-ar="نظام إدارة العملاء" data-en="Customer Management System">نظام إدارة العملاء</div>
    <div class="form-group">
      <label data-ar="🔐 كلمة السر" data-en="🔐 Password">🔐 كلمة السر</label>
      <input type="password" id="loginPassword" placeholder="ادخل كلمة السر" data-placeholder-ar="ادخل كلمة السر" data-placeholder-en="Enter password" onkeydown="if(event.key==='Enter') doLogin()">
    </div>
    <button class="btn" onclick="doLogin()" data-ar="دخول" data-en="Login">دخول</button>
    <div class="info-text" style="text-align:center; margin-top:12px;" data-ar="كلمة السر للحذف: 2026" data-en="Delete password: 2026">كلمة السر للحذف: 2026</div>
  </div>
</div>

<!-- ================================ -->
<!-- PAGE: HOME -->
<!-- ================================ -->
<div class="page" id="page-home">
  <div class="home-title" data-ar="أهلاً بك في Robica Store" data-en="Welcome to Robica Store">أهلاً بك في Robica Store</div>
  <div class="home-subtitle" data-ar="اختر نوع العملية" data-en="Choose operation type">اختر نوع العملية</div>
  <div class="home-cards">
    <div class="home-card" onclick="goTo('page-employee', 'new')">
      <div class="card-icon">🆕</div>
      <div class="card-title" data-ar="زبون جديد" data-en="New Customer">زبون جديد</div>
      <div class="card-desc" data-ar="تسجيل عميل لأول مرة وإضافة مشترياته" data-en="Register a new customer and add their purchase">تسجيل عميل لأول مرة وإضافة مشترياته</div>
    </div>
    <div class="home-card" onclick="goTo('page-employee', 'existing')">
      <div class="card-icon">👤</div>
      <div class="card-title" data-ar="زبون حالي" data-en="Existing Customer">زبون حالي</div>
      <div class="card-desc" data-ar="البحث عن عميل مسجل وإضافة عملية شراء جديدة" data-en="Search for a registered customer and add a new purchase">البحث عن عميل مسجل وإضافة عملية شراء جديدة</div>
    </div>
    <div class="home-card" onclick="goTo('page-history')">
      <div class="card-icon">📋</div>
      <div class="card-title" data-ar="سجل المشتريات" data-en="Purchase History">سجل المشتريات</div>
      <div class="card-desc" data-ar="عرض وإدارة جميع المعاملات" data-en="View and manage all transactions">عرض وإدارة جميع المعاملات</div>
    </div>
  </div>
</div>

<!-- ================================ -->
<!-- PAGE: EMPLOYEE SELECT -->
<!-- ================================ -->
<div class="page" id="page-employee">
  <div class="page-title" data-ar="👋 مين أنت؟" data-en="👋 Who are you?">👋 مين أنت؟</div>
  <div style="color:var(--text-muted);font-size:13px;text-align:center;margin-bottom:16px;" data-ar="اختر اسمك من القائمة" data-en="Select your name from the list">اختر اسمك من القائمة</div>

  <div class="employee-list" id="employeeList">
    <!-- Rendered by JS -->
  </div>

  <div class="add-employee-row" style="margin-top:20px;">
    <input type="text" id="newEmployeeInput" placeholder="اضافة موظف جديد..." data-placeholder-ar="اضافة موظف جديد..." data-placeholder-en="Add new employee...">
    <button class="btn btn-outline btn-sm" onclick="addEmployee()" data-ar="إضافة" data-en="Add">إضافة</button>
  </div>

  <button class="btn" style="max-width:300px;margin-top:20px;" onclick="confirmEmployee()" data-ar="متابعة" data-en="Continue">متابعة</button>
</div>

<!-- ================================ -->
<!-- PAGE: NEW CUSTOMER FORM -->
<!-- ================================ -->
<div class="page" id="page-new-customer">
  <div class="form-box">
    <div class="form-title" data-ar="🆕 تسجيل زبون جديد" data-en="🆕 Register New Customer">🆕 تسجيل زبون جديد</div>

    <div class="form-group">
      <label><span class="required-star">*</span><span data-ar="الاسم الكامل" data-en="Full Name">الاسم الكامل</span></label>
      <input type="text" id="nc-name" placeholder="اسم العميل" data-placeholder-ar="اسم العميل" data-placeholder-en="Customer name">
    </div>

    <div class="form-group">
      <label><span class="required-star">*</span><span data-ar="رقم التليفون" data-en="Phone Number">رقم التليفون</span></label>
      <input type="tel" id="nc-phone" placeholder="01XXXXXXXXX" dir="ltr">
    </div>

    <div class="row-2">
      <div class="form-group">
        <label><span class="required-star">*</span><span data-ar="النوع" data-en="Gender">النوع</span></label>
        <select id="nc-gender">
          <option value="" data-ar="اختر..." data-en="Select...">اختر...</option>
          <option value="ذكر" data-ar="ذكر" data-en="Male">ذكر</option>
          <option value="أنثى" data-ar="أنثى" data-en="Female">أنثى</option>
        </select>
      </div>
      <div class="form-group">
        <label><span class="optional-tag" data-ar="(اختياري)" data-en="(optional)">(اختياري)</span><span data-ar="تاريخ الميلاد" data-en="Date of Birth">تاريخ الميلاد</span></label>
        <input type="date" id="nc-dob">
      </div>
    </div>

    <div class="form-group">
      <label><span class="optional-tag" data-ar="(اختياري)" data-en="(optional)">(اختياري)</span><span data-ar="رقم من رشّحه (المرشِّح)" data-en="Referrer Phone">رقم من رشّحه (المرشِّح)</span></label>
      <input type="tel" id="nc-referrer" placeholder="01XXXXXXXXX" dir="ltr">
    </div>

    <div class="divider"></div>

    <div class="form-title" style="margin-bottom:20px;" data-ar="💰 تفاصيل المشتريات" data-en="💰 Purchase Details">💰 تفاصيل المشتريات</div>

    <div class="row-2">
      <div class="form-group">
        <label data-ar="السعر الأصلي (جنيه)" data-en="Original Price (EGP)">السعر الأصلي (جنيه)</label>
        <input type="number" id="nc-price" placeholder="0" min="0" oninput="calcPrice('nc')">
      </div>
      <div class="form-group">
        <label data-ar="الخصم (%)" data-en="Discount (%)">الخصم (%)</label>
        <input type="number" id="nc-discount" placeholder="0" min="0" max="100" oninput="calcPrice('nc')">
      </div>
    </div>

    <div class="price-display" id="nc-price-display" style="display:none;">
      <div class="price-row">
        <span data-ar="السعر الأصلي" data-en="Original Price">السعر الأصلي</span>
        <span class="val" id="nc-orig">—</span>
      </div>
      <div class="price-row">
        <span data-ar="الخصم" data-en="Discount">الخصم</span>
        <span class="val discount-val" id="nc-disc-val">—</span>
      </div>
      <div class="price-row final">
        <span data-ar="💳 المبلغ النهائي" data-en="💳 Final Amount">💳 المبلغ النهائي</span>
        <span class="val" id="nc-final">—</span>
      </div>
    </div>

    <div class="sheet-notice warning" data-ar="⚠️ تأكد من إعداد Google Sheets أولاً لحفظ البيانات هناك" data-en="⚠️ Make sure to setup Google Sheets first to save data there">⚠️ تأكد من إعداد Google Sheets أولاً لحفظ البيانات هناك</div>

    <button class="btn" style="margin-top:20px;" onclick="submitNewCustomer()" data-ar="✅ تسجيل العميل والمشتريات" data-en="✅ Register Customer & Purchase">✅ تسجيل العميل والمشتريات</button>
  </div>
</div>

<!-- ================================ -->
<!-- PAGE: EXISTING CUSTOMER SEARCH -->
<!-- ================================ -->
<div class="page" id="page-existing-search">
  <div class="form-box">
    <div class="form-title" data-ar="🔍 البحث عن زبون حالي" data-en="🔍 Search Existing Customer">🔍 البحث عن زبون حالي</div>
    <div class="form-group">
      <label data-ar="ابحث بالاسم أو رقم التليفون" data-en="Search by name or phone">ابحث بالاسم أو رقم التليفون</label>
      <input type="text" id="search-input" placeholder="اكتب هنا..." data-placeholder-ar="اكتب هنا..." data-placeholder-en="Type here..." oninput="searchCustomers()">
    </div>
    <div id="search-results"></div>
  </div>
</div>

<!-- ================================ -->
<!-- PAGE: SALE (for existing customer) -->
<!-- ================================ -->
<div class="page" id="page-sale">
  <div class="form-box">
    <div class="customer-profile" id="sale-profile">
      <!-- filled by JS -->
    </div>

    <div class="form-title" data-ar="💰 تسجيل مشتريات جديدة" data-en="💰 Register New Purchase">💰 تسجيل مشتريات جديدة</div>

    <div class="row-2">
      <div class="form-group">
        <label data-ar="السعر الأصلي (جنيه)" data-en="Original Price (EGP)">السعر الأصلي (جنيه)</label>
        <input type="number" id="sale-price" placeholder="0" min="0" oninput="calcPrice('sale')">
      </div>
      <div class="form-group">
        <label data-ar="الخصم (%)" data-en="Discount (%)">الخصم (%)</label>
        <input type="number" id="sale-discount" placeholder="0" min="0" max="100" oninput="calcPrice('sale')">
      </div>
    </div>

    <div class="price-display" id="sale-price-display" style="display:none;">
      <div class="price-row">
        <span data-ar="السعر الأصلي" data-en="Original Price">السعر الأصلي</span>
        <span class="val" id="sale-orig">—</span>
      </div>
      <div class="price-row">
        <span data-ar="الخصم" data-en="Discount">الخصم</span>
        <span class="val discount-val" id="sale-disc-val">—</span>
      </div>
      <div class="price-row final">
        <span data-ar="💳 المبلغ النهائي" data-en="💳 Final Amount">💳 المبلغ النهائي</span>
        <span class="val" id="sale-final">—</span>
      </div>
    </div>

    <button class="btn" style="margin-top:16px;" onclick="submitSale()" data-ar="✅ تسجيل المشتريات" data-en="✅ Register Purchase">✅ تسجيل المشتريات</button>
  </div>
</div>

<!-- ================================ -->
<!-- PAGE: HISTORY -->
<!-- ================================ -->
<div class="page" id="page-history">
  <div class="history-box">
    <div class="history-header">
      <div class="page-title" data-ar="📋 سجل المعاملات" data-en="📋 Transaction History">📋 سجل المعاملات</div>
      <button class="btn btn-outline btn-sm" onclick="loadHistory()" data-ar="🔄 تحديث" data-en="🔄 Refresh">🔄 تحديث</button>
    </div>

    <div class="filter-row">
      <input type="text" id="history-search" placeholder="بحث بالاسم أو رقم..." data-placeholder-ar="بحث بالاسم أو رقم..." data-placeholder-en="Search by name or phone..." oninput="filterHistory()">
      <select id="history-emp" onchange="filterHistory()">
        <option value="" data-ar="كل الموظفين" data-en="All Employees">كل الموظفين</option>
      </select>
    </div>

    <div style="overflow-x:auto;">
      <table class="history-table" id="historyTable">
        <thead>
          <tr>
            <th data-ar="العميل" data-en="Customer">العميل</th>
            <th data-ar="التليفون" data-en="Phone">التليفون</th>
            <th data-ar="الموظف" data-en="Employee">الموظف</th>
            <th data-ar="التاريخ" data-en="Date">التاريخ</th>
            <th data-ar="المبلغ" data-en="Amount">المبلغ</th>
            <th data-ar="النوع" data-en="Type">النوع</th>
            <th data-ar="حذف" data-en="Delete">حذف</th>
          </tr>
        </thead>
        <tbody id="historyBody">
          <tr><td colspan="7" class="empty-state"><div class="emoji">⌛</div><p>جاري التحميل...</p></td></tr>
        </tbody>
      </table>
    </div>

    <div id="historyStats" style="margin-top:20px;display:flex;gap:16px;flex-wrap:wrap;"></div>
  </div>
</div>

<!-- ================================ -->
<!-- DELETE MODAL -->
<!-- ================================ -->
<div class="modal-overlay" id="deleteModal">
  <div class="modal">
    <h3 data-ar="🗑️ حذف المعاملة" data-en="🗑️ Delete Transaction">🗑️ حذف المعاملة</h3>
    <p data-ar="ادخل كلمة سر الحذف (2026) لتأكيد الحذف" data-en="Enter the delete password (2026) to confirm">ادخل كلمة سر الحذف (2026) لتأكيد الحذف</p>
    <div class="form-group">
      <input type="password" id="deletePassword" placeholder="كلمة سر الحذف" data-placeholder-ar="كلمة سر الحذف" data-placeholder-en="Delete password" onkeydown="if(event.key==='Enter') confirmDelete()">
    </div>
    <div class="modal-btns">
      <button class="btn btn-outline" onclick="closeDeleteModal()" data-ar="إلغاء" data-en="Cancel">إلغاء</button>
      <button class="btn btn-red" onclick="confirmDelete()" data-ar="حذف" data-en="Delete">حذف</button>
    </div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
// =============================================
// STATE
// =============================================
let currentLang = 'ar';
let currentFlow = 'new'; // 'new' or 'existing'
let selectedEmployee = '';
let selectedCustomer = null;
let pageHistory = [];
let deleteTargetId = null;
let allTransactions = [];
let employees = JSON.parse(localStorage.getItem('robica_employees') || '["أحمد","محمد","سارة","مريم"]');

// =============================================
// LANGUAGE
// =============================================
function toggleLang() {
  currentLang = currentLang === 'ar' ? 'en' : 'ar';
  document.documentElement.lang = currentLang;
  document.documentElement.dir = currentLang === 'ar' ? 'rtl' : 'ltr';
  document.querySelector('.back-btn').textContent = currentLang === 'ar' ? '→ رجوع' : '← Back';
  applyLang();
}

function applyLang() {
  document.querySelectorAll('[data-ar]').forEach(el => {
    const txt = currentLang === 'ar' ? el.dataset.ar : el.dataset.en;
    if (txt) el.textContent = txt;
  });
  document.querySelectorAll('[data-placeholder-ar]').forEach(el => {
    el.placeholder = currentLang === 'ar' ? el.dataset.placeholderAr : el.dataset.placeholderEn;
  });
  // Select options
  document.querySelectorAll('select option[data-ar]').forEach(opt => {
    opt.textContent = currentLang === 'ar' ? opt.dataset.ar : opt.dataset.en;
  });
}

// =============================================
// NAVIGATION
// =============================================
function goTo(pageId, flow) {
  if (flow) currentFlow = flow;
  const current = document.querySelector('.page.active');
  if (current) pageHistory.push(current.id);

  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(pageId).classList.add('active');

  document.getElementById('backBtn').style.display = pageId === 'page-login' || pageId === 'page-home' ? 'none' : 'block';

  // Page-specific init
  if (pageId === 'page-employee') renderEmployees();
  if (pageId === 'page-history') loadHistory();
  if (pageId === 'page-existing-search') {
    document.getElementById('search-input').value = '';
    document.getElementById('search-results').innerHTML = '';
  }

  applyLang();
}

function goBack() {
  if (pageHistory.length === 0) { goTo('page-home'); return; }
  const prev = pageHistory.pop();
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(prev).classList.add('active');
  document.getElementById('backBtn').style.display = prev === 'page-login' || prev === 'page-home' ? 'none' : 'block';
  applyLang();
}

// =============================================
// LOGIN
// =============================================
function doLogin() {
  const pw = document.getElementById('loginPassword').value;
  if (pw === '1234') {
    goTo('page-home');
    pageHistory = [];
  } else {
    showToast(currentLang === 'ar' ? '❌ كلمة السر غلط' : '❌ Wrong password', 'error');
    document.getElementById('loginPassword').value = '';
  }
}

// =============================================
// EMPLOYEES
// =============================================
function renderEmployees() {
  const list = document.getElementById('employeeList');
  list.innerHTML = employees.map(emp =>
    `<button class="employee-btn ${selectedEmployee === emp ? 'selected' : ''}" onclick="selectEmployee('${emp}')">${emp}</button>`
  ).join('');
}

function selectEmployee(name) {
  selectedEmployee = name;
  renderEmployees();
}

function addEmployee() {
  const input = document.getElementById('newEmployeeInput');
  const name = input.value.trim();
  if (!name) return;
  if (!employees.includes(name)) {
    employees.push(name);
    localStorage.setItem('robica_employees', JSON.stringify(employees));
  }
  input.value = '';
  selectEmployee(name);
  renderEmployees();
}

function confirmEmployee() {
  if (!selectedEmployee) {
    showToast(currentLang === 'ar' ? 'اختر اسمك أولاً' : 'Please select your name', 'error');
    return;
  }
  document.getElementById('employeeName').textContent = selectedEmployee;
  document.getElementById('employeeBadge').style.display = 'inline-flex';

  if (currentFlow === 'new') {
    goTo('page-new-customer');
  } else {
    goTo('page-existing-search');
  }
}

// =============================================
// PRICE CALCULATOR
// =============================================
function calcPrice(prefix) {
  const price = parseFloat(document.getElementById(`${prefix}-price`).value) || 0;
  const discount = parseFloat(document.getElementById(`${prefix}-discount`).value) || 0;
  const discAmt = price * (discount / 100);
  const final = price - discAmt;

  const display = document.getElementById(`${prefix}-price-display`);

  if (price > 0) {
    display.style.display = 'block';
    document.getElementById(`${prefix}-orig`).textContent = `${price.toLocaleString()} جنيه`;
    document.getElementById(`${prefix}-disc-val`).textContent = discount > 0 ? `- ${discAmt.toLocaleString()} جنيه (${discount}%)` : '—';
    document.getElementById(`${prefix}-final`).textContent = `${final.toLocaleString()} جنيه`;
  } else {
    display.style.display = 'none';
  }
}

// =============================================
// SUBMIT NEW CUSTOMER
// =============================================
async function submitNewCustomer() {
  const name = document.getElementById('nc-name').value.trim();
  const phone = document.getElementById('nc-phone').value.trim();
  const gender = document.getElementById('nc-gender').value;
  const dob = document.getElementById('nc-dob').value;
  const referrer = document.getElementById('nc-referrer').value.trim();
  const price = parseFloat(document.getElementById('nc-price').value) || 0;
  const discount = parseFloat(document.getElementById('nc-discount').value) || 0;

  if (!name) { showToast(currentLang === 'ar' ? 'الاسم مطلوب' : 'Name is required', 'error'); return; }
  if (!phone) { showToast(currentLang === 'ar' ? 'رقم التليفون مطلوب' : 'Phone is required', 'error'); return; }
  if (!gender) { showToast(currentLang === 'ar' ? 'النوع مطلوب' : 'Gender is required', 'error'); return; }
  if (price <= 0) { showToast(currentLang === 'ar' ? 'ادخل المبلغ' : 'Enter the amount', 'error'); return; }

  const finalAmount = price - (price * discount / 100);
  const now = new Date();

  const record = {
    customerName: name,
    phone,
    gender,
    dob: dob || null,
    referrer: referrer || null,
    employee: selectedEmployee,
    originalPrice: price,
    discount,
    finalAmount,
    date: now.toLocaleDateString('ar-EG'),
    time: now.toLocaleTimeString('ar-EG'),
    timestamp: now.toISOString(),
    isNewCustomer: true
  };

  await saveRecord(record);
}

// =============================================
// SEARCH EXISTING CUSTOMER
// =============================================
async function searchCustomers() {
  const q = document.getElementById('search-input').value.trim().toLowerCase();
  const resultsDiv = document.getElementById('search-results');

  if (q.length < 2) { resultsDiv.innerHTML = ''; return; }

  if (!window.firebaseReady || !window.db) {
    resultsDiv.innerHTML = `<div class="search-result"><div class="res-name">⚠️ Firebase غير متصل بعد</div><div class="res-phone">أعد تحميل الصفحة أو أضف إعدادات Firebase</div></div>`;
    return;
  }

  try {
    const { collection, getDocs } = window.firestore;
    const snapshot = await getDocs(collection(window.db, 'transactions'));
    const customers = {};

    snapshot.forEach(doc => {
      const d = doc.data();
      const key = d.phone;
      if (!customers[key]) {
        customers[key] = { name: d.customerName, phone: d.phone, count: 0, total: 0 };
      }
      customers[key].count++;
      customers[key].total += d.finalAmount || 0;
    });

    const matches = Object.values(customers).filter(c =>
      c.name.toLowerCase().includes(q) || c.phone.includes(q)
    );

    if (matches.length === 0) {
      resultsDiv.innerHTML = `<div class="search-result"><div class="res-name" style="color:var(--text-muted)">لا توجد نتائج</div></div>`;
      return;
    }

    resultsDiv.innerHTML = matches.map(c => `
      <div class="search-result" onclick="selectExistingCustomer('${c.name}', '${c.phone}', ${c.count}, ${c.total})">
        <div class="res-name">${c.name}</div>
        <div class="res-phone">📞 ${c.phone}</div>
        <div class="res-purchases">عدد الزيارات: ${c.count} — إجمالي المشتريات: ${c.total.toLocaleString()} جنيه</div>
      </div>
    `).join('');
  } catch(e) {
    resultsDiv.innerHTML = `<div class="search-result"><div class="res-name">⚠️ خطأ في البحث</div></div>`;
  }
}

function selectExistingCustomer(name, phone, count, total) {
  selectedCustomer = { name, phone, count, total };
  document.getElementById('sale-profile').innerHTML = `
    <div class="profile-name">${name}</div>
    <div class="profile-info">
      <span>📞 <b>${phone}</b></span>
      <span>🛍️ ${currentLang === 'ar' ? 'عدد الزيارات' : 'Visits'}: <b>${count}</b></span>
      <span>💰 ${currentLang === 'ar' ? 'الإجمالي' : 'Total'}: <b>${total.toLocaleString()} جنيه</b></span>
    </div>
  `;
  document.getElementById('sale-price').value = '';
  document.getElementById('sale-discount').value = '';
  document.getElementById('sale-price-display').style.display = 'none';
  goTo('page-sale');
}

async function submitSale() {
  if (!selectedCustomer) return;
  const price = parseFloat(document.getElementById('sale-price').value) || 0;
  const discount = parseFloat(document.getElementById('sale-discount').value) || 0;

  if (price <= 0) { showToast(currentLang === 'ar' ? 'ادخل المبلغ' : 'Enter the amount', 'error'); return; }

  const finalAmount = price - (price * discount / 100);
  const now = new Date();

  const record = {
    customerName: selectedCustomer.name,
    phone: selectedCustomer.phone,
    employee: selectedEmployee,
    originalPrice: price,
    discount,
    finalAmount,
    date: now.toLocaleDateString('ar-EG'),
    time: now.toLocaleTimeString('ar-EG'),
    timestamp: now.toISOString(),
    isNewCustomer: false
  };

  await saveRecord(record);
}

// =============================================
// SAVE RECORD (Firebase + Google Sheets)
// =============================================
async function saveRecord(record) {
  if (!window.firebaseReady || !window.db) {
    showToast(currentLang === 'ar' ? '⚠️ Firebase غير متصل — أضف إعداداتك أولاً' : '⚠️ Firebase not connected', 'error');
    return;
  }

  try {
    const { collection, addDoc } = window.firestore;
    await addDoc(collection(window.db, 'transactions'), record);

    // Try Google Sheets
    await sendToGoogleSheets(record);

    showToast(currentLang === 'ar' ? '✅ تم الحفظ بنجاح!' : '✅ Saved successfully!', 'success');

    // Reset & go home
    setTimeout(() => {
      selectedCustomer = null;
      goTo('page-home');
      pageHistory = [];
    }, 1500);

  } catch(e) {
    showToast(currentLang === 'ar' ? '❌ خطأ في الحفظ: ' + e.message : '❌ Save error: ' + e.message, 'error');
  }
}

// =============================================
// GOOGLE SHEETS INTEGRATION
// =============================================
// ⚠️ يجب استبدال SHEET_ID و SCRIPT_URL بالقيم الخاصة بك
const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyVOZdGt1SaeFRLdKpWADpWox9Dx7bOtpcML-ZMhJ7mP-zQtbTq6rYREQPrZIBrOQ/exec';

async function sendToGoogleSheets(record) {
  if (GOOGLE_SCRIPT_URL === 'YOUR_GOOGLE_APPS_SCRIPT_URL') return; // Skip if not configured
  try {
    await fetch(GOOGLE_SCRIPT_URL, {
      method: 'POST',
      mode: 'no-cors',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(record)
    });
  } catch(e) {
    console.warn('Google Sheets sync failed:', e);
  }
}

// =============================================
// HISTORY
// =============================================
async function loadHistory() {
  const tbody = document.getElementById('historyBody');
  tbody.innerHTML = `<tr><td colspan="7" style="text-align:center;padding:40px;color:var(--text-muted)">⌛ جاري التحميل...</td></tr>`;

  if (!window.firebaseReady || !window.db) {
    tbody.innerHTML = `<tr><td colspan="7" style="text-align:center;padding:40px;color:var(--red)">⚠️ Firebase غير متصل</td></tr>`;
    return;
  }

  try {
    const { collection, getDocs, orderBy, query } = window.firestore;
    const q = query(collection(window.db, 'transactions'), orderBy('timestamp', 'desc'));
    const snapshot = await getDocs(q);

    allTransactions = [];
    snapshot.forEach(d => allTransactions.push({ id: d.id, ...d.data() }));

    // Update employee filter
    const empSelect = document.getElementById('history-emp');
    const empSet = new Set(allTransactions.map(t => t.employee).filter(Boolean));
    empSelect.innerHTML = `<option value="">كل الموظفين</option>` +
      [...empSet].map(e => `<option value="${e}">${e}</option>`).join('');

    renderHistory(allTransactions);
  } catch(e) {
    tbody.innerHTML = `<tr><td colspan="7" style="text-align:center;color:var(--red)">❌ ${e.message}</td></tr>`;
  }
}

function filterHistory() {
  const search = document.getElementById('history-search').value.toLowerCase();
  const emp = document.getElementById('history-emp').value;
  const filtered = allTransactions.filter(t => {
    const matchSearch = !search ||
      (t.customerName || '').toLowerCase().includes(search) ||
      (t.phone || '').includes(search);
    const matchEmp = !emp || t.employee === emp;
    return matchSearch && matchEmp;
  });
  renderHistory(filtered);
}

function renderHistory(transactions) {
  const tbody = document.getElementById('historyBody');

  if (transactions.length === 0) {
    tbody.innerHTML = `<tr><td colspan="7"><div class="empty-state"><div class="emoji">📭</div><p>لا توجد معاملات</p></div></td></tr>`;
    return;
  }

  tbody.innerHTML = transactions.map(t => `
    <tr>
      <td><b>${t.customerName || '—'}</b></td>
      <td dir="ltr">${t.phone || '—'}</td>
      <td>${t.employee || '—'}</td>
      <td style="font-size:11px;">${t.date || '—'}<br><span style="color:var(--text-muted)">${t.time || ''}</span></td>
      <td><b style="color:var(--gold)">${(t.finalAmount || 0).toLocaleString()} ج</b>${t.discount > 0 ? `<br><span style="font-size:10px;color:var(--text-muted)">كان ${(t.originalPrice||0).toLocaleString()} (${t.discount}% خصم)</span>` : ''}</td>
      <td><span class="badge ${t.isNewCustomer ? 'badge-new' : 'badge-existing'}">${t.isNewCustomer ? 'جديد' : 'حالي'}</span></td>
      <td><button class="delete-btn" onclick="openDeleteModal('${t.id}')">🗑️</button></td>
    </tr>
  `).join('');

  // Stats
  const totalRevenue = transactions.reduce((s, t) => s + (t.finalAmount || 0), 0);
  const totalSavings = transactions.reduce((s, t) => s + ((t.originalPrice || 0) - (t.finalAmount || 0)), 0);
  document.getElementById('historyStats').innerHTML = `
    <div style="background:var(--dark2);border:1px solid rgba(201,168,76,0.15);padding:16px 24px;flex:1;min-width:160px;">
      <div style="font-size:11px;color:var(--text-muted);margin-bottom:4px;">إجمالي المبيعات</div>
      <div style="font-size:22px;font-weight:700;color:var(--gold)">${totalRevenue.toLocaleString()} ج</div>
    </div>
    <div style="background:var(--dark2);border:1px solid rgba(201,168,76,0.15);padding:16px 24px;flex:1;min-width:160px;">
      <div style="font-size:11px;color:var(--text-muted);margin-bottom:4px;">عدد المعاملات</div>
      <div style="font-size:22px;font-weight:700;color:var(--white)">${transactions.length}</div>
    </div>
    <div style="background:var(--dark2);border:1px solid rgba(201,168,76,0.15);padding:16px 24px;flex:1;min-width:160px;">
      <div style="font-size:11px;color:var(--text-muted);margin-bottom:4px;">إجمالي الخصومات</div>
      <div style="font-size:22px;font-weight:700;color:var(--red)">${totalSavings.toLocaleString()} ج</div>
    </div>
  `;
}

// =============================================
// DELETE
// =============================================
function openDeleteModal(id) {
  deleteTargetId = id;
  document.getElementById('deletePassword').value = '';
  document.getElementById('deleteModal').classList.add('open');
}

function closeDeleteModal() {
  deleteTargetId = null;
  document.getElementById('deleteModal').classList.remove('open');
}

async function confirmDelete() {
  const pw = document.getElementById('deletePassword').value;
  if (pw !== '2026') {
    showToast(currentLang === 'ar' ? '❌ كلمة سر الحذف غلط' : '❌ Wrong delete password', 'error');
    return;
  }

  if (!deleteTargetId || !window.db) return;

  try {
    const { doc, deleteDoc } = window.firestore;
    await deleteDoc(doc(window.db, 'transactions', deleteTargetId));
    closeDeleteModal();
    showToast(currentLang === 'ar' ? '🗑️ تم الحذف' : '🗑️ Deleted', 'success');
    loadHistory();
  } catch(e) {
    showToast('❌ ' + e.message, 'error');
  }
}

// =============================================
// TOAST
// =============================================
function showToast(msg, type = '') {
  const toast = document.getElementById('toast');
  toast.textContent = msg;
  toast.className = 'toast ' + type + ' show';
  setTimeout(() => toast.className = 'toast ' + type, 3000);
}

// =============================================
// INIT
// =============================================
document.addEventListener('firebaseReady', () => {
  console.log('✅ Firebase connected');
});

applyLang();
</script>
</body>
</html>
